# Project-local registry override.
# The machine-level ~/.npmrc points at an internal registry; this project ships zero
# dependencies, so scope any incidental npm use to the public registry here rather
# than editing machine-level config.
registry=https://registry.npmjs.org/
